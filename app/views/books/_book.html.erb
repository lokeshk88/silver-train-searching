
    <th scope="row">#</th>
    <td><%= book.isbn %></td>
    <td><%= book.name %></td>
    <td><%= book.author %></td>
    <td><%= book.year %></td>
    <td><%= book.price %></td>
    <td><%= book.status %></td>
    <td><%= book.genre %></td>
    <th>
    <td><%= link_to "Show", book, class:"btn btn-success mb-2" %></td>
    <td><%= link_to "Edit", edit_book_path(book), class: "btn btn-warning mb-2" %> </td> 
    <td><%= button_to "Delete", book, method: :delete, class: "btn btn-danger mb-2" %></td>
    </th>
